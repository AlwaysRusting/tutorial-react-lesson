import React from 'react';
import InputField, { Props } from '.';

const props: Props = {};

export default {
  title: 'Atoms',
};

export const basicInputField = () => <InputField {...props} />;
